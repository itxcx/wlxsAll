<view class="activity">
      <view class="activeItem" wx:for="{{activityList}}" wx:key="unique">
          <image src="{{item.path}}"></image>
      </view>
      <view class="more">暂无活动</view>
</view>